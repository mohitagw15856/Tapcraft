# TapCraft — Core (required)
pyyaml>=6.0
pynput>=1.7.6

# Optional: Microphone-based slap detection
# Uncomment the two lines below if you want mic slap detection.
# If you get build errors, try: pip install sounddevice --only-binary=:all:
# sounddevice>=0.4.6
# numpy>=1.24.0
